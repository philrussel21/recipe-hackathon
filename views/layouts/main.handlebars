<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/build/styles.css" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://api.mapbox.com/mapbox-gl-js/v1.12.0/mapbox-gl.js"></script>
    <link
      href="https://api.mapbox.com/mapbox-gl-js/v1.12.0/mapbox-gl.css"
      rel="stylesheet"
    />
    <title>Orbis Foods</title>
  </head>

  <body class="h-screen">
    <div class="flex flex-col h-full justify-between bg-gray-600 items-center">
      <header class="flex justify-center h-16 w-full">
        <nav class="flex flex-row justify-between w-9/12 relative">
          <div
            class="left-nav flex flex-row justify-evenly items-center w-3/12 h-full"
          >
            <a class="nav-items" href="/">Orbis Foods</a>
          </div>
          <div
            class="mid-nav mb-auto w-6/12 h-full flex flex-row justify-end items-center pr-4"
          >
           

            <img
              id="search-icon"
              class="cursor-pointer"
              src="/icons/search.svg"
              alt="Search Icon"
            />
             <a class="nav-items mx-4 bg-teal-400 hover:bg-white text-black font-bold py-2 px-4 rounded-full" href="/recipes/new">
             <svg class="w-6 h-6 inline-block" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path></svg>
             Add Recipe</a>
          </div>
          <div
            class="flex flex-row justify-evenly items-center right-nav w-3/12 h-full"
          >
            {{#if user}}
                <a class="nav-items bg-yellow-300 hover:bg-gray-100 text-black font-semibold py-2 px-4 border border-gray-400 rounded shadow" href="/users/{{user.email}}">{{user.username}}</a>
                <a class="nav-items bg-red-500 hover:bg-gray-100 text-black font-semibold py-2 px-4 border border-gray-400 rounded shadow" href="/users/logout">Logout</a>

            {{else}}
            {{!-- <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg> --}}
                <a class="nav-items bg-green-300 hover:bg-gray-100 text-black font-semibold py-2 px-4 border border-gray-400 rounded shadow" href="/users/register">
                <svg class="w-6 h-6 inline-block" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path></svg>
                Sign Up</a>

                <a class="nav-items bg-blue-500 hover:bg-gray-100 text-black font-semibold py-2 px-4 border border-gray-400 rounded shadow" href="/users/login">
                <svg class="w-6 h-6 inline-block" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                Log In</a>
            {{/if}}
          </div>
        </nav>
      </header>
      <main class="flex justify-center bg-black h-full w-full overflow-y-auto">
        <section id="main-section" class="w-9/12 h-full">{{{body}}}</section>
      </main>
      <footer class="w-9/12 h-12 sticky">
      </footer>
    </div>

    {{!-- TODO: Add function to show search bar when icon is clicked  --}}

    <script>
        const search = document.getElementById("search-icon");
        search.addEventListener('click', () => {

      })
    </script>
  </body>
</html>
